{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the osheep desktop window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
